//sizeof returns the sizeof array
 (_term_offset < sizeof(_term))

 //bool used like an if statement
  bool_func= c == '*'; // i can use this in my switch so that if the value sent over the wire is == 'mychar' then then bool_func becomes true

  enum {kojo, kwesi, junior};
  string family_member = kojo;

  //enum is constants and the value change only be changes using pointers 

  strcmp // compares strings

